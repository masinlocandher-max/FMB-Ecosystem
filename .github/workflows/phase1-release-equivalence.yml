name: Phase 1 release equivalence

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  compare-release-paths:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repair branch
        uses: actions/checkout@v4
      - name: Checkout current main baseline
        uses: actions/checkout@v4
        with:
          ref: main
          path: baseline-main
      - uses: actions/setup-node@v7
        with:
          node-version: 20
      - name: Install repair-branch dependencies
        run: npm install --no-fund
      - name: Install baseline dependencies
        working-directory: baseline-main
        run: npm install --no-fund
      - name: Audit five-stage release contract
        run: |
          node -e "const p=require('./package.json'); const expected=['validate:content','build:apps','generate:news','apply:shared-shell','verify:dist']; for (const name of expected) { if (!p.scripts[name]) throw new Error('missing '+name); } if ((p.scripts.build.match(/post-build-/g)||[]).length) throw new Error('package build still contains post-build ledger'); console.log(p.scripts.build);"
      - name: Build current main release baseline
        working-directory: baseline-main
        run: |
          release_stamp="$(git show -s --format=%cI HEAD)"
          FMB_BUILD_TIMESTAMP="$release_stamp" NODE_OPTIONS="--require=$GITHUB_WORKSPACE/scripts/freeze-build-clock.cjs" npm run build
      - name: Build five-stage repair candidate
        run: |
          release_stamp="$(git -C baseline-main show -s --format=%cI HEAD)"
          FMB_BUILD_TIMESTAMP="$release_stamp" NODE_OPTIONS="--require=$GITHUB_WORKSPACE/scripts/freeze-build-clock.cjs" npm run build
      - name: Compare final outputs byte-for-byte
        run: node scripts/compare-release-paths.mjs baseline-main/dist dist
      - name: Print exact remaining differences
        if: failure()
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = require('path');
          const jsonFiles = [
            'assets/images/home/home-image-manifest.json',
            'enterprise-audit.json',
            'performance-audit.json',
          ];
          function diffJson(a, b, prefix = '') {
            const out = [];
            if (Object.is(a, b)) return out;
            if (Array.isArray(a) && Array.isArray(b)) {
              const max = Math.max(a.length, b.length);
              for (let i = 0; i < max; i++) out.push(...diffJson(a[i], b[i], `${prefix}[${i}]`));
              return out;
            }
            if (a && b && typeof a === 'object' && typeof b === 'object' && !Array.isArray(a) && !Array.isArray(b)) {
              const keys = [...new Set([...Object.keys(a), ...Object.keys(b)])].sort();
              for (const key of keys) out.push(...diffJson(a[key], b[key], prefix ? `${prefix}.${key}` : key));
              return out;
            }
            out.push({ key: prefix || '<root>', baseline: a, candidate: b });
            return out;
          }
          function compactHtmlDiff(baseline, candidate) {
            let prefix = 0;
            const limit = Math.min(baseline.length, candidate.length);
            while (prefix < limit && baseline.charCodeAt(prefix) === candidate.charCodeAt(prefix)) prefix += 1;
            let suffix = 0;
            while (
              suffix < baseline.length - prefix &&
              suffix < candidate.length - prefix &&
              baseline.charCodeAt(baseline.length - 1 - suffix) === candidate.charCodeAt(candidate.length - 1 - suffix)
            ) suffix += 1;
            const context = 180;
            return {
              prefixChars: prefix,
              suffixChars: suffix,
              baselineChanged: baseline.slice(prefix, baseline.length - suffix),
              candidateChanged: candidate.slice(prefix, candidate.length - suffix),
              beforeContext: baseline.slice(Math.max(0, prefix - context), prefix),
              afterContext: baseline.slice(baseline.length - suffix, Math.min(baseline.length, baseline.length - suffix + context)),
            };
          }
          for (const file of jsonFiles) {
            const baseline = JSON.parse(fs.readFileSync(path.join('baseline-main/dist', file), 'utf8'));
            const candidate = JSON.parse(fs.readFileSync(path.join('dist', file), 'utf8'));
            const changes = diffJson(baseline, candidate);
            const payload = Buffer.from(JSON.stringify(changes)).toString('base64');
            console.log(`::warning title=FMB_JSON_DIFF_${file.replace(/[^A-Za-z0-9]/g,'_')}::${payload}`);
          }
          for (const file of ['news/amor-deloso-public-record/index.html','news/pbbm-sona-2026-analysis/index.html']) {
            const baseline = fs.readFileSync(path.join('baseline-main/dist', file), 'utf8');
            const candidate = fs.readFileSync(path.join('dist', file), 'utf8');
            const payload = Buffer.from(JSON.stringify(compactHtmlDiff(baseline, candidate))).toString('base64');
            console.log(`::warning title=FMB_HTML_DIFF_${file.replace(/[^A-Za-z0-9]/g,'_')}::${payload}`);
          }
          NODE
